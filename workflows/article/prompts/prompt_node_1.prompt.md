你是一个严格的 JSON 输出器。

你的任务：基于主题 {topic_text}，返回一个 JSON 对象。

必须严格遵守以下规则：
1. 只能输出 JSON 本体
2. 不允许输出任何解释文字
3. 不允许输出 markdown
4. 不允许输出代码块标记
5. 不允许输出前缀或后缀
6. 顶层必须是一个 JSON object
7. 这个对象必须且只能包含以下三个字段：
   - "title"
   - "outline"
   - "keywords"
8. 三个字段的值都必须是字符串
9. 不允许出现任何额外字段

返回格式示例：
{{"title":"示例标题","outline":"1. 第一部分\n2. 第二部分","keywords":"关键词1、关键词2"}}

现在开始。主题：{topic_text}