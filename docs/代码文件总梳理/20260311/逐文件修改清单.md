可以。按你最新拍板，唯一修正是：

* **完整 Raw JSON 保留**
* 其余仍按前面那套收口执行

下面直接给你**逐文件修改清单**。我按“删什么、改什么、保留什么”写，不讲空话。

---

# 第一批：先清主命名和 compat 壳

## 1. `frontend-react/src/run/runTypes.ts`

### 改

* 正式请求体命名统一到：

  * `TestSubgraphRequestPayload` 或 `SubgraphTestRequestPayload`
* 文件注释里所有主命名改成：

  * `subgraph test`
  * 不再把 `partial execution` 当正式主名

### 删

* `PartialExecutionRequestPayload`
* `TestSubgraphRequestPayload = PartialExecutionRequestPayload` 这种 alias 结构
* 所有把 `partial execution` 作为正式 transport 名称的注释

### 保留

* `RunResult`
* `RunScope = 'full' | 'subgraph'`
* `final_state / partial_state`
* step projection 类型

---

## 2. `frontend-react/src/api.ts`

### 改

* 新增/保留唯一正式函数：

  * `runTestSubgraphWorkflow(...)`
    或你想统一成 `runSubgraphTestWorkflow(...)`，但只保留一个

### 删

* `runPartialExecutionWorkflow(...)`
* `runPartialWorkflow(...)`
* 相关旧注释

### 保留

* `runDraftWorkflow(...)`
* save/load/delete 等 workflow API
* `DEFAULT_CANVAS_ID` 暂时可以保留，不是这一轮核心

---

## 3. `frontend-react/src/workflow-editor/operations/workflowEditorOperations.ts`

### 改

* 把节点测试操作统一命名为：

  * `runTestSubgraphResult(...)` 或 `runSubgraphTestResult(...)`
* 结果字段统一命名为：

  * `subgraphTestResult`
  * 不再出现 `partialExecutionResult` / `partialRunResult`

### 删

* `PartialExecutionWorkflowResult`
* `PartialWorkflowResult`
* `runPartialExecutionWorkflowResult(...)`
* `runPartialWorkflowResult(...)`

### 顺手收紧

* 去掉这类静默兜底：

  * `response.warnings || []`
  * `response.sidecar || EMPTY_WORKFLOW_SIDECAR`
  * bootstrap 里的 `|| []` / `|| ''`
* 改成结果 shape 自己明确返回，别在这里偷偷修

### 保留

* `fetchWorkflowBootstrapResult`
* `fetchWorkflowDetailResult`
* `saveWorkflowResult`
* `runDraftWorkflowResult`

---

## 4. `frontend-react/src/workflow-editor/controllers/useWorkflowRuntime.ts`

### 改

把整套主命名统一成一套，例如：

* `subgraphTestState`
* `activeSubgraphTestResult`
* `activeSubgraphTestStartNodeId`
* `subgraphTestResultsByNodeId`
* `staleSubgraphTestResultIds`
* `runningSubgraphTestNodeId`
* `lastSuccessfulSubgraphTestStartNodeId`

方法统一成：

* `handleRunSubgraphTest`
* `markSubgraphTestResultStale`
* `clearSubgraphTestResultStale`
* `clearSubgraphTestResult`
* `pruneSubgraphTestArtifacts`
* `resetSubgraphTestState`
* `resetSubgraphTestContext`

### 删

* 所有 `partialExecution*` 主命名
* 所有 `partialRun*` alias/export
* return 里的双出口旧名：

  * `activePartialRunResult`
  * `activePartialStartNodeId`
  * `partialRunResultsByNodeId`
  * `stalePartialRunResultIds`
  * `testingNodeId`
  * `lastSuccessfulPartialStartNodeId`
  * `markPartialRunResultStale`
  * `clearPartialRunResultStale`
  * `handleRunPartial`
  * `clearPartialRunResult`

### 删第二层修补器

* `normalizeWorkflowSidecarNodeAssets`
* `hasWorkflowSidecarNodeAssetsContent`
* `normalizeWorkflowSidecarData`

这些不该继续待在 runtime controller 里当本地 normalize 层。

### 保留

* `cloneWorkflowState`
* sidecar state owner 本身
* runInputs owner
* save/load/run 主控制能力

---

# 第二批：清前端 `coerce` 修补链

## 5. `frontend-react/src/workflow-editor/domain/workflowEditorConfig.ts`

### 删

* `coerceOutputSpecsForUI`
* `coerceInputNodeConfigForUI`
* `coercePromptNodeConfigForUI`
* `coerceOutputNodeConfigForUI`
* `coerceEditorNodeConfigForUI`

### 保留

* `createInitialWorkflowNodeConfig`
* `buildDefaultOutputSpecs`

### 改

* 文件职责改成纯“初始值工厂”
* 不再写“轻量字段收敛层”

---

## 6. `frontend-react/src/workflow-editor/domain/WorkflowEditorNodeFactory.ts`

### 删

* `coerceEditorNodeForUI`
* `getDefaultConfig`
* `buildInitialConfigByType`

### 改

* `createNodeByType(...)` 直接调用 `createInitialWorkflowNodeConfig(...)`
* 不再经过任何 `coerce` 包装

### 保留

* `createNodeByType`

---

## 7. `frontend-react/src/workflow-editor/actions/workflowEditorActions.ts`

### 删

* `coerceEditorNodeForUI` import
* `buildUpdateNodeResult(...)` 里对 `coerceEditorNodeForUI(...)` 的调用

### 改

* `buildUpdateNodeResult(...)` 直接使用 `updatedNode`
* 节点类型不可切换，不再靠 `coerce` 静默回退旧 config
* 如果要拦，显式报错，不要偷偷恢复旧值

### 还要顺手改

* 文件里的 subgraph test 相关注释，去掉 `partial execution` 主命名

### 保留

* 其余 graph action orchestration

---

# 第三批：节点本体瘦身，只保留轻入口

## 8. `frontend-react/src/workflow-editor/workflowEditorGraphTypes.ts`

### 删

* `PartialExecutionNodeStatus`
* `PartialExecutionNodeSummary`
* `lastPartialExecutionStatus`
* `isPartialExecutionResultStale`
* `isPartialExecutionTarget`
* `partialExecutionSummary`

### 改

* `PartialExecutionActionHandler` 改名为：

  * `SubgraphTestActionHandler` 或 `NodeTestActionHandler`

* `onRequestPartialExecution` 改成：

  * `onRequestSubgraphTest` 或 `onRequestNodeTest`

* `isPartialExecutionRunning` 改成：

  * `isSubgraphTestRunning` 或 `isNodeTestRunning`

### 删历史噪音

* `PersistedWorkflowEdge`
* `PersistedWorkflowContextLink`
* `DerivedInputKeys`

### 保留

* config
* full run display 字段
* graph-derived display 字段
* 轻量测试入口和运行态

---

## 9. `frontend-react/src/workflow-editor/state/workflowEditorViewState.ts`

### 删

* `buildPartialExecutionPrimaryState`
* `buildPartialExecutionFailureSummary`
* `buildPartialExecutionSummary`
* `buildPartialExecutionFields` 中所有 summary/stale/target/last-status 派生
* 本文件底部的：

  * `buildInputNodes`
  * `buildSelectedNode`

### 改

* `BuildDisplayNodesOptions` 统一改名到 `subgraph test` 体系
* `buildDisplayNodes(...)` 只往 node.data 塞：

  * `onRequestSubgraphTest`
  * `isSubgraphTestRunning`

### 保留

* `buildExecutedNodeMap`
* `buildDisplayNodes`
* `buildDisplayEdges`
* full run runtime 派生
* graph-derived 派生

---

## 10. `frontend-react/src/components/WorkflowNode.tsx`

### 删

整块测试详情展示：

* `renderPartialExecutionSummaryBlock`
* `getPartialSummaryStyles`
* `lastPartialExecutionStatus` 相关显示
* `isPartialExecutionResultStale` 相关 chip
* `isPartialExecutionTarget` 相关显示
* `partialExecutionSummary` 相关显示

### 删 output 智能去重花活

* `stableJsonStringify`
* `areEquivalentJsonValues`
* `isOutputAggregationPreview`
* `isOutputPublishedStateRedundant`

### 改

* `onRequestPartialExecution` 改名
* `isPartialExecutionRunning` 改名
* 按钮文案改成 `Test`，不是 `Partial`

### 保留

* 节点配置摘要
* handles
* inbound bindings
* graph window summary
* `Test` 按钮
* `Running...` 状态

---

# 第四批：右侧面板彻底收口成两块

## 11. `frontend-react/src/components/NodeConfigPanel.tsx`

### 删 props

* `allNodes`
* `allNodes: _allNodes`

### 删本地 fallback 重算

* `trim`
* `buildUniqueStrings`
* `buildInboundBindings`
* `buildDerivedTargetInputs`
* `buildGraphWindowFacts`

### 删所有这类写法

* `node.data.xxx ?? 本地重算`

### 删整块 sidecar extras

* `Node Assets / Sidecar Extras`
* `Pinned Prompt Context`
* `Opaque Metadata`

### 删调试透视

* `Reusable Partial Execution State` 主展示区
  注意：不是把状态逻辑删掉，而是**不在主面板展示**
* 各种重复 stale/status/info 说明块

### 改命名

* `Partial Execution` 区块改成 `Node Test` 或 `Subgraph Test`
* `effectivePartialExecutionInputItems` 改成正式命名
* `onRunPartialExecution` 改名
* `selectedPartialExecutionDisplayRun` 改名
* `runningPartialExecutionNodeId` 改名
* `partialExecutionErrorMessage` / `InfoMessage` 改名

### 结果区最终只保留

* Status
* Failure Summary
* Primary State
* Steps（折叠）
* **Raw JSON（保留）**

### 保留

* Node ID
* Node Type
* config 表单
* Comment
* Delete Node
* Pinned Inputs 编辑
* Run Test
* Clear Cached Result
* Reset Reusable Context
* Raw JSON

---

# 第五批：graph/display owner 唯一化

## 12. `frontend-react/src/workflow-editor/state/workflowEditorRunInputs.ts`

### 保留

* `buildInputNodes`
* `getRunInputKey`
* `buildNextRunInputs`

### 配套删除

* 其他文件里重复实现的 `buildInputNodes`

---

## 13. `frontend-react/src/workflow-editor/controllers/useWorkflowGraphEditor.ts`

### 改

* options 里的全部节点测试命名统一成 `subgraph test`
* `buildDisplayNodes(...)` 调用参数统一成新命名
* 删掉注释里“暂时保留旧字段名映射”这种过渡说明

### 删

* 所有 `partialRunResultsByNodeId` / `stalePartialRunResultIds` 旧映射桥

### 保留

* graph controller 本体
* displayNodes / displayEdges 主装配

---

# 第六批：graph 层和 validation 层去重复

## 14. `frontend-react/src/workflow-editor/domain/workflowEditorGraph.ts`

### 删

* 本文件里的 `hasExecutionPath`

### 改

* 连接 edge/contextLink 的 cycle 判断改为调用共享 helper

### 保留

* graph 层即时预阻断
* rename / delete / connect 联动
* output rename 与 context consistency 这些 graph-specific 逻辑

### 另外删掉

* `getPrimaryStateKey`
* `previousOutput`
* `nextOutput`
* `outputChanged`
  这些如果外面没人真正用，就一起删；现在看基本是过度返回字段

---

## 15. `frontend-react/src/workflow-editor/domain/workflowEditorValidationRules.ts`

### 删

* 本文件里的 `hasExecutionPath`

### 改

* cycle 检查改为复用共享 helper
* context outbound 规则继续保留，但只保留一份 rule owner

### 保留

* `validateOutputFormat`
* `validateEdgeReferences`
* `validateNodeOutputRules`
* `validateNodeInboundBindingRules`
* `validateContextLinkReferences`

---

## 16. 新增一个共享 helper 文件

建议新增，例如：

* `frontend-react/src/workflow-editor/domain/workflowEditorExecutionGraph.ts`

### 放进去

* 统一的执行关系图构建 helper
* 统一的 `hasExecutionPath(...)`

这样前端只有一份 cycle/path 遍历实现。

---

# 第七批：页面层命名和展示清理

## 17. `frontend-react/src/components/WorkflowEditor.tsx`

### 改命名

把整套节点测试状态统一改成：

* `isSubgraphTestPanelExpanded`

* `requestedSubgraphTestNodeId`

* `subgraphTestPanelErrorMessage`

* `subgraphTestInfoMessage`

* `subgraphTestState`

* `activeSubgraphTestResult`

* `activeSubgraphTestStartNodeId`

* `subgraphTestResultsByNodeId`

* `staleSubgraphTestResultIds`

* `runningSubgraphTestNodeId`

* `lastSuccessfulSubgraphTestStartNodeId`

* `buildEffectiveSubgraphTestInputItems`

* `buildMergedSubgraphTestState`

### 改调用

* `requestPartialExecutionFromCanvas` 改名
* `handleRunSelectedPartialExecution` 改名
* `handleClearSelectedPartialExecutionResult` 改名
* `handleResetPartialExecutionReusableContext` 改名

### 删

* 页面里所有 `partial execution` 主文案和主变量名
* 只保留 Raw JSON 在结果区，不再保留多余 debug 块

### 保留

* full run panel
* Node Config / Node Test 面板分区
* pinned inputs 主链
* reusable context 逻辑本身（但不主展示）

---

## 18. `frontend-react/src/components/RunResultPanel.tsx`

### 保留

* `Raw JSON`

### 改

* 如果有 `partial execution` 文案，全部改成 `subgraph test` / `Node Test`
* 仅保留 displayRun 展示，不额外解释 transport

### 不删

* 你刚拍板了，完整 Raw JSON 要保留

---

# 第八批：类型与命名噪音清理

## 19. `frontend-react/src/workflow-editor/workflowEditorTypes.ts`

### 改名

* `WorkflowApiNode` -> `WorkflowNode`
* `WorkflowApiEdge` -> `WorkflowEdge`

### 保留

* 其余 canonical mirror 结构
* `OutputNodeConfig` 当前仍叫 output，这一轮不动

---

## 20. `frontend-react/src/workflow-editor/workflowEditorUiTypes.ts`

### 删

如果项目里没有主链依赖，直接删：

* `WorkflowPageContext`
* `WorkflowRunContextMatch`
* `EdgeClickSelectionResult`
* `PaneClickSelectionResult`
* `NodeClickSelectionResult`
* `SelectionChangeResult`
* `SelectionResult`

### 改

* `WorkflowSidecarNodeAssets` 继续保留字段，但 UI 主面板只消费 `pinnedInputs`
* 节点测试相关类型命名统一到 `subgraph test`

### 保留

* `WorkflowLoadWarning`
* `WorkflowSidecarNodeAssets`
* `WorkflowSidecarData`
* `RuntimeActionResult`
* `WorkflowRunContext`

---

# 第九批：后端命名收口

## 21. `api/run_http_schemas.py`

### 改

* 注释与说明里把正式主命名收口到：

  * `subgraph test`
* `partial execution` 从主注释退出

### 你可以选其一

#### 方案 A：小改

保留类名：

* `TestSubgraphRequest`

只改注释文案

#### 方案 B：彻底统一

改类名：

* `SubgraphTestRequest`

我建议你如果这轮要一口气清，就直接上 B。

### 保留

* `RunResult`
* `run_scope='subgraph'`

---

## 22. `api/run_result_mapper.py`

### 改

* 注释里不再写 `partial execution`
* 统一写 `subgraph test`

### 保留

* 映射逻辑本体

---

# 第十批：后端 validator 和前端镜像对齐

## 23. `contracts/step_projections.py`

### 保留

* 全部保留
* 不属于这一轮删除重点

## 24. `contracts/workflow_contracts.py`

### 保留

* 全部保留
* 这轮只把它当 canonical owner，不改

## 25. `workflow validator.py`

### 保留

* 后端正式 validator 主体

### 只要做一件事

* 注释/命名里如果还把 `partial execution` 当主名，统一收口到 `subgraph test`

---

# 你最后前端会剩什么

## 右侧只剩两块

### `Node Config`

* canonical config 编辑

### `Node Test`

* 输入
* Pinned Default
* Run Test
* Clear Cached Result
* Reset Reusable Context
* Status
* Failure Summary
* Primary State
* Steps
* **Raw JSON**

## 节点本体只剩

* Test 按钮
* Running 状态

## sidecar 主面板只剩

* `pinnedInputs`

---

# 最后给你一个执行顺序

你说不要暂缓，那就按这个顺序一次性推：

1. `runTypes.ts`
2. `api.ts`
3. `workflowEditorOperations.ts`
4. `useWorkflowRuntime.ts`
5. `workflowEditorConfig.ts`
6. `WorkflowEditorNodeFactory.ts`
7. `workflowEditorActions.ts`
8. `workflowEditorGraphTypes.ts`
9. `workflowEditorViewState.ts`
10. `WorkflowNode.tsx`
11. `NodeConfigPanel.tsx`
12. `workflowEditorRunInputs.ts`
13. `useWorkflowGraphEditor.ts`
14. `workflowEditorGraph.ts`
15. `workflowEditorValidationRules.ts`
16. 新共享 execution-graph helper
17. `WorkflowEditor.tsx`
18. `RunResultPanel.tsx`
19. `workflowEditorTypes.ts`
20. `workflowEditorUiTypes.ts`
21. `run_http_schemas.py`
22. `run_result_mapper.py`
23. 全仓注释 / 命名统一扫尾

如果你要，我下一条就直接把这个清单再压成**“第一批必须动的 10 个文件 + 每个文件一句改动目标”**。
